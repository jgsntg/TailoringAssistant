Resume from the last handoff.

1. Read `.ai/handoffs/CURRENT.md`. If it doesn't exist, list `.ai/handoffs/` and read the most recent dated file.
2. Read `AGENTS.md` for project conventions.
3. Run `git status` and `git log -5 --oneline` to verify the repo state matches what the handoff describes. Flag any mismatch.
4. Summarize back to me: the goal, current status, your understanding of next steps, and any concerns about the handoff (stale info, missing context, decisions you'd push back on).
5. Wait for my confirmation before editing anything.
